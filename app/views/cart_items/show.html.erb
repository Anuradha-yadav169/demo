<h1>CartItems#show</h1>
<p>Find me in app/views/cart_items/show.html.erb</p>
<h2>Cart show</h2>
<ul>
  
    <ul>
      <li>
        <%= link_to "Add one", product_url(:id => product_url), method: :post %>
        <%= link_to "Reduce one", product_reduce_path(:id => product), method: :post %>
      </li>
      <li>
        <%= link_to "Remove item", product_path(product), method: :delete %>
      </li>
    </ul>
    #
 
</ul>
<h3>Sub total: <%= number_to_currency(@cart.sub_total) %></h3>
# See Order section below for explanation of new_order_path
<%= link_to "Proceed to checkout", new_order_path %><br>
#
<%= link_to "Empty cart", cart_item_path(@current_cart_item),method: :delete, data: {confirm: "Are you sure?"}
